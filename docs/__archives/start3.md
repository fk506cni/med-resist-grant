# セッション開始プロンプト: 敵対的レビュー（第3回）

以下の指示に従い、本プロジェクトの実装準備状況と資料整備状況について敵対的レビューを行ってください。レビュー結果は `docs/report3.md` に出力してください。

## レビュー方針

- **実装を開始しないでください。** レビューと指摘、およびその対応のみを行います。
- **研究内容のレビューは不要です。** refs/ 以下の研究資料には触れないでください。
- **敵対的に検証してください。** 「うまくいきそう」ではなく「どこで壊れるか」を探してください。
- **docs/report2.md を読まずに**独立してレビューを行い、レポート作成時に report2.md と突き合わせて所見を統合してください。
- レビュー結果は docs/report3.md に、重大度（Critical / Major / Minor / Note）付きで出力してください。
- 前回レビューとの差分（新規発見 / 既知だが未対応 / 前回から改善済み）を明示してください。

## 前回からの主な変化

- Docker環境を構築済み（docker/python/Dockerfile, docker/docker-compose.yml）
  - python:3.12-slim + Pandoc 3.6.4 + python-docx/openpyxl/pyyaml/ruamel.yaml/Jinja2 + fonts-noto-cjk
  - ビルド・パッケージimport・Pandocバージョン・ソースファイル参照・UID/GID 全て検証済み
- SPEC.md の docker-compose.yml 記述を実ファイルに合わせて修正済み
- docs/prompts.md の Prompt 0-1 完了チェックを全項目チェック済みに更新

## レビュー対象

### 1. Docker環境の実用性検証

Docker環境は構築済みですが、**実際のパイプラインで使える状態か**を検証してください:

- Dockerfileの内容と docker-compose.yml を読み、設計に問題がないか確認
- 以下の実運用シナリオで不足がないか検証:
  - `fill_forms.py` が data/source/*.docx をコピー→編集→output/ に書き出す際のパーミッション
  - `build_narrative.sh` が pandoc コマンドを実行する際の reference-doc パス解決
  - Makefile からの `docker compose run` 呼び出しパターン（未作成のMakefileがどう書かれるべきか）
- ruamel.yaml の C拡張（ruamel.yaml.clib）がインストールされているか確認
  （pip install ruamel.yaml だけでは C拡張が入らない場合がある）

### 2. prompts.md の実装プロンプト品質検証

各 Prompt が「そのままAIに渡して実装できるレベルか」を厳密に検証してください:

#### 情報の十分性
- 各 Prompt の「参照」セクションで指定されたファイルを実際に読めば、実装に必要な情報が全て得られるか
- セルマッピング表（Prompt 3-1等）の行・列番号は実際の様式ファイルと一致するか
  - python-docx でテーブルをダンプして突合すること
- YAML スキーマ設計（Prompt 1-1〜1-3）のフィールドは、対応する様式の全記入欄を網羅しているか

#### 実行順序と依存関係
- Prompt 間の依存関係は明示されているか（例: Prompt 3-1 は Prompt 1-1 の完了を前提とする）
- 各 Prompt の完了チェックは検証可能か（曖昧な基準になっていないか）

#### 抜け漏れ
- Makefile の作成を指示する Prompt が存在するか
- reference-doc（Pandoc用カスタムdocx）の作成手順は十分か
- パッケージングスクリプト（scripts/create_package.sh）の作成を指示する Prompt が存在するか

### 3. ドキュメント間の整合性（再々検証）

前回のレビュー＋Docker環境構築を経た現在の4ファイルを突き合わせてください:
- `CLAUDE.md`
- `SPEC.md`
- `README.md`
- `docs/prompts.md`

特に:
- Docker環境に関する記述が4ファイル間で整合しているか（サービス名、ボリュームマウント、実行コマンド例）
- SPEC.md §4.1 の docker-compose.yml 記述が実ファイル（docker/docker-compose.yml）と一致するか
- prompts.md の Prompt 0-1 完了チェックが全てチェック済みになっているか
- 出力ファイル名（_filled, _narrative サフィックス）が全ドキュメントで依然として統一されているか

### 4. パイプライン全体の実行可能性評価

実際にコードを書かずに、以下の「思考実験」で各ステップの実行可能性を検証してください:

#### Makefile 設計
- prompts.md に Makefile 作成の指示があるか確認し、なければ必要なターゲットを洗い出す
- `make build` が呼ぶべきステップの順序と依存関係

#### Windows連携
- Docker (Linux) で生成した docx を Windows の Word COM API で修復→PDF化するフロー
- Google Drive 同期の設計（scripts/sync_gdrive.sh が未作成であること自体のリスク評価）

#### エンドツーエンドテスト
- data/dummy/ を使ったE2Eテストの設計は十分か
- 「ダミーデータで全パイプラインを通す」テストシナリオの具体性

### 5. リスクマトリクス（更新版）

Docker環境構築完了を踏まえ、残存リスクを再評価してください:

- **技術リスク**: python-docx/openpyxl/Pandocの技術的制約に起因するリスク
- **要件リスク**: 募集要項の解釈誤りに起因するリスク
- **スケジュールリスク**: 提出期限(5/20)までの残り46日に対する作業量のリスク
- **環境リスク**: Docker/Windows/Google Drive連携に起因するリスク
- **設計リスク**: 現在の設計判断が実装フェーズで問題を引き起こすリスク

### 6. report2.md との突合（レポート作成時のみ）

レポート作成の最終段階で docs/report2.md を読み、以下を確認してください:
- 前回「対応済み」とされた項目が現在も修正状態を維持しているか
- 前回「未対応（実装フェーズで対応）」とされた項目（C2-03, C2-11, C2-12, C2-13）の現在の状況
  - 特に C2-03（Docker環境）は今回の環境構築で解消されたか
- 今回のレビューで新規発見した問題と、前回の既知問題との重複・関連
- 前回のレビューで見落とされていた問題がないか

## 出力フォーマット

`docs/report3.md` に以下の形式で出力してください:

```markdown
# 敵対的レビュー報告書（第3回）

レビュー実施日: YYYY-MM-DD
レビュー対象: （対象ファイル一覧）
前回レビュー: docs/report2.md (2026-04-03)

## サマリ

- Critical: N件 (新規N / 既知未対応N)
- Major: N件 (新規N / 既知未対応N)
- Minor: N件 (新規N / 既知未対応N)
- Note: N件

### 対応状況一覧

| ID | 重大度 | 状態 | 概要 |
|----|------|------|------|
| C3-01 | Critical | ... | ... |

## report2.md との差分サマリ

- report2.md の未対応項目で今回解消されたもの: N件
- report2.md の未対応項目で依然として未対応のもの: N件
- report2.md に記載がなく今回新規発見したもの: N件

## 指摘事項

### [C3-01] (Critical) タイトル
- **箇所**: ファイル名:行番号 or セクション名
- **前回対応状況**: 新規 / report2.md [C2-NN] 対応済み / report2.md [C2-NN] 未対応
- **内容**: 具体的な問題の説明
- **影響**: この問題が放置された場合に起きること
- **推奨対応**: 修正方針

## リスクマトリクス

| リスク | 影響度 | 発生確率 | 総合評価 | 対策 |
|-------|------|--------|--------|-----|
| ... | 高/中/低 | 高/中/低 | Critical/Major/Minor | ... |
```

重大度の基準:
- **Critical**: 実装がブロックされる、または提出物に致命的欠陥が生じる
- **Major**: 実装に手戻りが発生する、または提出物の品質に重大な影響がある
- **Minor**: 修正すべきだが実装を進めながら対応可能
- **Note**: 改善推奨だが現状でも問題なく進められる
